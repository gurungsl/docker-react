version: 2.1

jobs:
  build:
    working_directory: ~/myapp
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      - run: 
          commnad: npm install --save
      - run: 
          command: npm run start
workflows:
  build_and_test:
    jobs:
      - build      


  
    
